{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2023"],
    "module": "ESNext",
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "Bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true
  },
  "include": ["vite.config.ts"]
}

// {
//   "compilerOptions": {
//     // Кэш-файл, в котором хранятся промежуточные данные компиляции
//     // Ускоряет повторные сборки
//     "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",
//     // Целевая версия JavaScript — поддерживает новейшие возможности ES2022
//     "target": "ES2022",
//     // Указывает, какие глобальные API доступны (например, `Promise`, `Intl`)
//     "lib": [
//       "ES2023"
//     ],
//     // Указывает, как компилировать модули — ESNext используется для ESM (import/export)
//     "module": "ESNext",
//     // Пропускает проверку типов в node_modules — ускоряет компиляцию
//     "skipLibCheck": true,
//     // 🔧 Используется для сборки с Vite, Webpack, Rollup и пр.
//     // Поддержка import из .ts, .tsx и т.п.
//     "moduleResolution": "bundler", // Специальный режим для Vite и современных бандлеров
//     "allowImportingTsExtensions": true, // Разрешает импорты с расширением `.ts`, например: `import './config.ts'`
//     "verbatimModuleSyntax": true, // Сохраняет `import/export` без преобразования
//     "moduleDetection": "force", // Явно указывает, что каждый файл — это модуль (если есть import/export)
//     "noEmit": true, // Не создавать `.js`-файлы на выходе (только типизация, Vite всё сам соберёт)
//     // 🧹 Строгие проверки (для чистоты кода и отлова ошибок)
//     "strict": true, // Включает все строгие флаги (ниже — частные уточнения)
//     "noUnusedLocals": true, // Предупреждение/ошибка при неиспользуемых переменных
//     "noUnusedParameters": true, // Предупреждение при неиспользуемых аргументах в функциях
//     "erasableSyntaxOnly": true, // Проверяет только синтаксис, который можно безопасно удалить
//     "noFallthroughCasesInSwitch": true, // Предотвращает случайные переходы между case'ами в switch
//     "noUncheckedSideEffectImports": true // Запрещает импорты с возможными побочными эффектами без использования
//   },
//   // Указываем, какие файлы включаются в проверку (в данном случае — только конфиг Vite)
//   "include": [
//     "vite.config.ts"
//   ]
// }